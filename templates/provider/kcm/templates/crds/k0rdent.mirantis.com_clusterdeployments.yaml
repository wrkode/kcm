---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: clusterdeployments.k0rdent.mirantis.com
spec:
  group: k0rdent.mirantis.com
  names:
    kind: ClusterDeployment
    listKind: ClusterDeploymentList
    plural: clusterdeployments
    shortNames:
    - clusterd
    - cld
    singular: clusterdeployment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Shows readiness of the ClusterDeployment
      jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - description: Number of ready out of total services
      jsonPath: .status.conditions[?(@.type=="ServicesInReadyState")].message
      name: Services
      type: string
    - description: ClusterTemplate used for the ClusterDeployment
      jsonPath: .spec.template
      name: Template
      type: string
    - description: Shows either readiness or error messages from child objects
      jsonPath: .status.conditions[?(@.type=="Ready")].message
      name: Messages
      type: string
    - description: Time elapsed since object creation
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: Dry Run
      jsonPath: .spec.dryRun
      name: DryRun
      priority: 1
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterDeployment is the Schema for the ClusterDeployments API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterDeploymentSpec defines the desired state of ClusterDeployment
            properties:
              config:
                description: |-
                  Config allows to provide parameters for template customization.
                  If no Config provided, the field will be populated with the default values for
                  the template and DryRun will be enabled.
                x-kubernetes-preserve-unknown-fields: true
              credential:
                description: Name reference to the related Credentials object.
                type: string
              dryRun:
                description: DryRun specifies whether the template should be applied
                  after validation or only validated.
                type: boolean
              propagateCredentials:
                default: true
                description: |-
                  PropagateCredentials indicates whether credentials should be propagated
                  for use by CCM (Cloud Controller Manager).
                type: boolean
              serviceSpec:
                description: ServiceSpec is spec related to deployment of services.
                properties:
                  continueOnError:
                    default: false
                    description: ContinueOnError specifies if the services deployment
                      should continue if an error occurs.
                    type: boolean
                  driftExclusions:
                    description: DriftExclusions specifies specific configurations
                      of resources to ignore for drift detection.
                    items:
                      properties:
                        paths:
                          description: Paths is a slice of JSON6902 paths to exclude
                            from configuration drift evaluation.
                          items:
                            type: string
                          type: array
                        target:
                          description: Target points to the resources that the paths
                            refers to.
                          properties:
                            annotationSelector:
                              description: |-
                                AnnotationSelector is a string that follows the label selection expression
                                https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#api
                                It matches with the resource annotations.
                              type: string
                            group:
                              description: |-
                                Group is the API group to select resources from.
                                Together with Version and Kind it is capable of unambiguously identifying and/or selecting resources.
                                https://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md
                              type: string
                            kind:
                              description: |-
                                Kind of the API Group to select resources from.
                                Together with Group and Version it is capable of unambiguously
                                identifying and/or selecting resources.
                                https://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md
                              type: string
                            labelSelector:
                              description: |-
                                LabelSelector is a string that follows the label selection expression
                                https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#api
                                It matches with the resource labels.
                              type: string
                            name:
                              description: Name to match resources with.
                              type: string
                            namespace:
                              description: Namespace to select resources from.
                              type: string
                            version:
                              description: |-
                                Version of the API Group to select resources from.
                                Together with Group and Kind it is capable of unambiguously identifying and/or selecting resources.
                                https://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md
                              type: string
                          type: object
                      required:
                      - paths
                      type: object
                    type: array
                  driftIgnore:
                    description: DriftIgnore specifies resources to ignore for drift
                      detection.
                    items:
                      properties:
                        annotationSelector:
                          description: |-
                            AnnotationSelector is a string that follows the label selection expression
                            https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#api
                            It matches with the resource annotations.
                          type: string
                        group:
                          description: |-
                            Group is the API group to select resources from.
                            Together with Version and Kind it is capable of unambiguously identifying and/or selecting resources.
                            https://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md
                          type: string
                        kind:
                          description: |-
                            Kind of the API Group to select resources from.
                            Together with Group and Version it is capable of unambiguously
                            identifying and/or selecting resources.
                            https://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md
                          type: string
                        labelSelector:
                          description: |-
                            LabelSelector is a string that follows the label selection expression
                            https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#api
                            It matches with the resource labels.
                          type: string
                        name:
                          description: Name to match resources with.
                          type: string
                        namespace:
                          description: Namespace to select resources from.
                          type: string
                        version:
                          description: |-
                            Version of the API Group to select resources from.
                            Together with Group and Kind it is capable of unambiguously identifying and/or selecting resources.
                            https://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md
                          type: string
                      type: object
                    type: array
                  priority:
                    default: 100
                    description: |-
                      Priority sets the priority for the services defined in this spec.
                      Higher value means higher priority and lower means lower.
                      In case of conflict with another object managing the service,
                      the one with higher priority will get to deploy its services.
                    format: int32
                    maximum: 2147483646
                    minimum: 1
                    type: integer
                  reload:
                    description: Reload instances via rolling upgrade when a ConfigMap/Secret
                      mounted as volume is modified.
                    type: boolean
                  services:
                    description: |-
                      Services is a list of services created via ServiceTemplates
                      that could be installed on the target cluster.
                    items:
                      description: Service represents a Service to be deployed.
                      properties:
                        disable:
                          description: Disable can be set to disable handling of this
                            service.
                          type: boolean
                        name:
                          description: Name is the chart release.
                          maxLength: 253
                          minLength: 1
                          type: string
                        namespace:
                          description: |-
                            Namespace is the namespace the release will be installed in.
                            It will default to Name if not provided.
                          type: string
                        template:
                          description: Template is a reference to a Template object
                            located in the same namespace.
                          maxLength: 253
                          minLength: 1
                          type: string
                        templateChain:
                          description: |-
                            TemplateChain defines the ServiceTemplateChain object that will be used to deploy the service
                            along with desired ServiceTemplate version.
                          type: string
                        values:
                          description: |-
                            Values is the helm values to be passed to the chart used by the template.
                            The string type is used in order to allow for templating.
                          type: string
                        valuesFrom:
                          description: ValuesFrom can reference a ConfigMap or Secret
                            containing helm values.
                          items:
                            properties:
                              kind:
                                description: |-
                                  Kind of the resource. Supported kinds are:
                                  - ConfigMap/Secret
                                enum:
                                - ConfigMap
                                - Secret
                                type: string
                              name:
                                description: |-
                                  Name of the referenced resource.
                                  Name can be expressed as a template and instantiate using any cluster field.
                                minLength: 1
                                type: string
                              namespace:
                                description: |-
                                  Namespace of the referenced resource.
                                  For ClusterProfile namespace can be left empty. In such a case, namespace will
                                  be implicit set to cluster's namespace.
                                  For Profile namespace must be left empty. The Profile namespace will be used.
                                  Namespace can be expressed as a template and instantiate using any cluster field.
                                type: string
                              optional:
                                default: false
                                description: |-
                                  Optional indicates that the referenced resource is not mandatory.
                                  If set to true and the resource is not found, the error will be ignored,
                                  and Sveltos will continue processing other ValueFroms.
                                type: boolean
                            required:
                            - kind
                            - name
                            type: object
                          type: array
                      required:
                      - name
                      - template
                      type: object
                    type: array
                  stopOnConflict:
                    default: false
                    description: |-
                      StopOnConflict specifies what to do in case of a conflict.
                      E.g. If another object is already managing a service.
                      By default the remaining services will be deployed even if conflict is detected.
                      If set to true, the deployment will stop after encountering the first conflict.
                    type: boolean
                  syncMode:
                    default: Continuous
                    description: SyncMode specifies how services are synced in the
                      target cluster.
                    enum:
                    - OneTime
                    - Continuous
                    - ContinuousWithDriftDetection
                    - DryRun
                    type: string
                  templateResourceRefs:
                    description: |-
                      TemplateResourceRefs is a list of resources to collect from the management cluster,
                      the values from which can be used in templates.
                    items:
                      properties:
                        identifier:
                          description: |-
                            Identifier is how the resource will be referred to in the
                            template
                          type: string
                        optional:
                          default: false
                          description: |-
                            Optional indicates that the referenced resource is not mandatory.
                            If set to true and the resource is not found, the error will be ignored,
                            and Sveltos will continue processing other TemplateResourceRefs.
                          type: boolean
                        resource:
                          description: |-
                            Resource references a Kubernetes instance in the management
                            cluster to fetch and use during template instantiation.
                            For ClusterProfile namespace can be left empty. In such a case, namespace will
                            be implicit set to cluster's namespace.
                            Name and namespace can be expressed as a template and instantiate using any cluster field.
                          properties:
                            apiVersion:
                              description: API version of the referent.
                              type: string
                            fieldPath:
                              description: |-
                                If referring to a piece of an object instead of an entire object, this string
                                should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                                For example, if the object reference is to a container within a pod, this would take on a value like:
                                "spec.containers{name}" (where "name" refers to the name of the container that triggered
                                the event) or if no container name is specified "spec.containers[2]" (container with
                                index 2 in this pod). This syntax is chosen only to have some well-defined way of
                                referencing a part of an object.
                              type: string
                            kind:
                              description: |-
                                Kind of the referent.
                                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                              type: string
                            name:
                              description: |-
                                Name of the referent.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            namespace:
                              description: |-
                                Namespace of the referent.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                              type: string
                            resourceVersion:
                              description: |-
                                Specific resourceVersion to which this reference is made, if any.
                                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                              type: string
                            uid:
                              description: |-
                                UID of the referent.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - identifier
                      - resource
                      type: object
                    type: array
                type: object
              template:
                description: Template is a reference to a Template object located
                  in the same namespace.
                maxLength: 253
                minLength: 1
                type: string
            required:
            - template
            type: object
          status:
            description: ClusterDeploymentStatus defines the observed state of ClusterDeployment
            properties:
              availableUpgrades:
                description: |-
                  AvailableUpgrades is the list of ClusterTemplate names to which
                  this cluster can be upgraded. It can be an empty array, which means no upgrades are
                  available.
                items:
                  type: string
                type: array
              conditions:
                description: Conditions contains details for the current state of
                  the ClusterDeployment.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              k8sVersion:
                description: |-
                  Currently compatible exact Kubernetes version of the cluster. Being set only if
                  provided by the corresponding ClusterTemplate.
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation.
                format: int64
                type: integer
              services:
                description: Services contains details for the state of services.
                items:
                  description: ServiceStatus contains details for the state of services.
                  properties:
                    clusterName:
                      description: ClusterName is the name of the associated cluster.
                      type: string
                    clusterNamespace:
                      description: ClusterNamespace is the namespace of the associated
                        cluster.
                      type: string
                    conditions:
                      description: Conditions contains details for the current state
                        of managed services.
                      items:
                        description: Condition contains details for one aspect of
                          the current state of this API Resource.
                        properties:
                          lastTransitionTime:
                            description: |-
                              lastTransitionTime is the last time the condition transitioned from one status to another.
                              This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                            format: date-time
                            type: string
                          message:
                            description: |-
                              message is a human readable message indicating details about the transition.
                              This may be an empty string.
                            maxLength: 32768
                            type: string
                          observedGeneration:
                            description: |-
                              observedGeneration represents the .metadata.generation that the condition was set based upon.
                              For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                              with respect to the current state of the instance.
                            format: int64
                            minimum: 0
                            type: integer
                          reason:
                            description: |-
                              reason contains a programmatic identifier indicating the reason for the condition's last transition.
                              Producers of specific condition types may define expected values and meanings for this field,
                              and whether the values are considered a guaranteed API.
                              The value should be a CamelCase string.
                              This field may not be empty.
                            maxLength: 1024
                            minLength: 1
                            pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                            type: string
                          status:
                            description: status of the condition, one of True, False,
                              Unknown.
                            enum:
                            - "True"
                            - "False"
                            - Unknown
                            type: string
                          type:
                            description: type of condition in CamelCase or in foo.example.com/CamelCase.
                            maxLength: 316
                            pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                            type: string
                        required:
                        - lastTransitionTime
                        - message
                        - reason
                        - status
                        - type
                        type: object
                      type: array
                  required:
                  - clusterName
                  type: object
                type: array
              servicesUpgradePaths:
                description: ServicesUpgradePaths contains details for the state of
                  services upgrade paths.
                items:
                  description: ServiceUpgradePaths contains details for the state
                    of service upgrade paths.
                  properties:
                    availableUpgrades:
                      description: AvailableUpgrades contains details for the state
                        of available upgrades.
                      items:
                        description: UpgradePath contains details for the state of
                          service upgrade paths.
                        properties:
                          upgradePaths:
                            description: Versions contains the list of versions that
                              service can be upgraded to.
                            items:
                              type: string
                            type: array
                        type: object
                      type: array
                    name:
                      description: Name is the name of the service.
                      type: string
                    namespace:
                      description: Namespace is the namespace of the service.
                      type: string
                    template:
                      description: Template is the name of the current service template.
                      type: string
                  required:
                  - name
                  - namespace
                  - template
                  type: object
                type: array
            type: object
        type: object
    served: false
    storage: false
    subresources:
      status: {}
  - additionalPrinterColumns:
    - description: Shows readiness of the ClusterDeployment
      jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - description: Number of ready out of total services
      jsonPath: .status.conditions[?(@.type=="ServicesInReadyState")].message
      name: Services
      type: string
    - description: ClusterTemplate used for the ClusterDeployment
      jsonPath: .spec.template
      name: Template
      type: string
    - description: Shows either readiness or error messages from child objects
      jsonPath: .status.conditions[?(@.type=="Ready")].message
      name: Messages
      type: string
    - description: Time elapsed since object creation
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: Dry Run
      jsonPath: .spec.dryRun
      name: DryRun
      priority: 1
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ClusterDeployment is the Schema for the ClusterDeployments API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterDeploymentSpec defines the desired state of ClusterDeployment
            properties:
              adoptedCluster:
                description: |-
                  AdoptedCluster defines configuration for adopted cluster lifecycle management.
                  This enables full infrastructure lifecycle management for adopted clusters.
                properties:
                  discoveryMode:
                    default: auto
                    description: DiscoveryMode defines how infrastructure should be
                      discovered.
                    enum:
                    - auto
                    - manual
                    type: string
                  enabled:
                    default: false
                    description: Enabled indicates whether adopted cluster lifecycle
                      management is enabled.
                    type: boolean
                  infrastructureMapping:
                    description: InfrastructureMapping defines the mapping of existing
                      infrastructure.
                    properties:
                      credentials:
                        description: Credentials specifies the credentials for cloud
                          provider access.
                        properties:
                          namespace:
                            default: default
                            description: Namespace specifies the namespace where the
                              secret is located.
                            type: string
                          secretName:
                            description: SecretName specifies the name of the secret
                              containing cloud credentials.
                            type: string
                        required:
                        - secretName
                        type: object
                      nodeMapping:
                        description: NodeMapping defines how existing nodes should
                          be mapped to CAPI Machine resources.
                        items:
                          description: NodeMapping defines how an existing node should
                            be mapped to a CAPI Machine resource.
                          properties:
                            infrastructureRef:
                              description: InfrastructureRef specifies the infrastructure
                                reference for the machine.
                              properties:
                                apiVersion:
                                  description: APIVersion specifies the API version
                                    of the infrastructure resource.
                                  type: string
                                kind:
                                  description: Kind specifies the kind of the infrastructure
                                    resource.
                                  type: string
                                name:
                                  description: Name specifies the name of the infrastructure
                                    resource.
                                  type: string
                              required:
                              - apiVersion
                              - kind
                              - name
                              type: object
                            machineName:
                              description: MachineName specifies the name for the
                                generated CAPI Machine resource.
                              type: string
                            nodeName:
                              description: NodeName specifies the name of the existing
                                node.
                              type: string
                          required:
                          - machineName
                          - nodeName
                          type: object
                        type: array
                      provider:
                        description: Provider specifies the cloud provider for the
                          adopted cluster.
                        enum:
                        - aws
                        - azure
                        - gcp
                        - vsphere
                        - openstack
                        - unknown
                        type: string
                      region:
                        description: Region specifies the cloud region where the cluster
                          is located.
                        type: string
                    required:
                    - provider
                    type: object
                  lifecycleManagement:
                    description: LifecycleManagement defines lifecycle management
                      capabilities.
                    properties:
                      backup:
                        description: Backup defines backup capabilities for the adopted
                          cluster.
                        properties:
                          enabled:
                            default: false
                            description: Enabled indicates whether backup is enabled.
                            type: boolean
                          retention:
                            description: Retention specifies how long to retain backups.
                            type: string
                          schedule:
                            description: Schedule specifies the backup schedule in
                              cron format.
                            type: string
                          storage:
                            description: Storage defines backup storage configuration.
                            properties:
                              credentials:
                                description: Credentials specifies credentials for
                                  storage access.
                                properties:
                                  namespace:
                                    default: default
                                    description: Namespace specifies the namespace
                                      where the secret is located.
                                    type: string
                                  secretName:
                                    description: SecretName specifies the name of
                                      the secret containing cloud credentials.
                                    type: string
                                required:
                                - secretName
                                type: object
                              location:
                                description: Location specifies the storage location.
                                type: string
                              type:
                                description: Type specifies the storage type.
                                enum:
                                - s3
                                - gcs
                                - azure
                                - local
                                type: string
                            required:
                            - location
                            - type
                            type: object
                        required:
                        - enabled
                        type: object
                      enabled:
                        default: false
                        description: Enabled indicates whether lifecycle management
                          is enabled.
                        type: boolean
                      monitoring:
                        description: Monitoring defines monitoring capabilities for
                          the adopted cluster.
                        properties:
                          enabled:
                            default: false
                            description: Enabled indicates whether monitoring is enabled.
                            type: boolean
                          healthChecks:
                            description: HealthChecks defines health check configurations.
                            properties:
                              enabled:
                                default: true
                                description: Enabled indicates whether health checks
                                  are enabled.
                                type: boolean
                              failureThreshold:
                                default: 3
                                description: FailureThreshold specifies the number
                                  of failures before marking unhealthy.
                                format: int32
                                minimum: 1
                                type: integer
                              interval:
                                default: 30s
                                description: Interval specifies the health check interval.
                                type: string
                              timeout:
                                default: 10s
                                description: Timeout specifies the health check timeout.
                                type: string
                            required:
                            - enabled
                            type: object
                          metrics:
                            description: Metrics defines which metrics to collect.
                            items:
                              type: string
                            type: array
                        required:
                        - enabled
                        type: object
                      scaling:
                        description: Scaling defines scaling capabilities for the
                          adopted cluster.
                        properties:
                          autoScaling:
                            default: false
                            description: AutoScaling indicates whether auto-scaling
                              is enabled.
                            type: boolean
                          enabled:
                            default: false
                            description: Enabled indicates whether scaling is enabled.
                            type: boolean
                          maxNodes:
                            description: MaxNodes specifies the maximum number of
                              nodes.
                            format: int32
                            minimum: 1
                            type: integer
                          minNodes:
                            description: MinNodes specifies the minimum number of
                              nodes.
                            format: int32
                            minimum: 1
                            type: integer
                          nodeGroups:
                            description: NodeGroups defines different node groups
                              for scaling.
                            items:
                              description: NodeGroup defines a group of nodes with
                                similar characteristics.
                              properties:
                                instanceType:
                                  description: InstanceType specifies the instance
                                    type for new nodes.
                                  type: string
                                labels:
                                  additionalProperties:
                                    type: string
                                  description: Labels specifies labels to apply to
                                    nodes in this group.
                                  type: object
                                maxNodes:
                                  description: MaxNodes specifies the maximum number
                                    of nodes in this group.
                                  format: int32
                                  minimum: 1
                                  type: integer
                                minNodes:
                                  description: MinNodes specifies the minimum number
                                    of nodes in this group.
                                  format: int32
                                  minimum: 0
                                  type: integer
                                name:
                                  description: Name specifies the name of the node
                                    group.
                                  type: string
                                taints:
                                  description: Taints specifies taints to apply to
                                    nodes in this group.
                                  items:
                                    description: Taint defines a taint to apply to
                                      nodes.
                                    properties:
                                      effect:
                                        description: Effect specifies the taint effect.
                                        enum:
                                        - NoSchedule
                                        - PreferNoSchedule
                                        - NoExecute
                                        type: string
                                      key:
                                        description: Key specifies the taint key.
                                        type: string
                                      value:
                                        description: Value specifies the taint value.
                                        type: string
                                    required:
                                    - effect
                                    - key
                                    type: object
                                  type: array
                              required:
                              - name
                              type: object
                            type: array
                        required:
                        - autoScaling
                        - enabled
                        type: object
                      upgrades:
                        description: Upgrades defines upgrade capabilities for the
                          adopted cluster.
                        properties:
                          autoUpgrade:
                            default: false
                            description: AutoUpgrade indicates whether automatic upgrades
                              are enabled.
                            type: boolean
                          enabled:
                            default: false
                            description: Enabled indicates whether upgrades are enabled.
                            type: boolean
                          maintenanceWindow:
                            description: MaintenanceWindow specifies the maintenance
                              window for upgrades.
                            type: string
                          maxUnavailable:
                            description: MaxUnavailable specifies the maximum number
                              of unavailable nodes during upgrade.
                            format: int32
                            minimum: 1
                            type: integer
                          upgradeStrategy:
                            default: rolling
                            description: UpgradeStrategy specifies the upgrade strategy
                              to use.
                            enum:
                            - rolling
                            - in-place
                            type: string
                        required:
                        - autoUpgrade
                        - enabled
                        type: object
                    required:
                    - enabled
                    type: object
                required:
                - enabled
                type: object
              config:
                description: |-
                  Config allows to provide parameters for template customization.
                  If no Config provided, the field will be populated with the default values for
                  the template and DryRun will be enabled.
                x-kubernetes-preserve-unknown-fields: true
              credential:
                description: Name reference to the related Credentials object.
                type: string
              dryRun:
                description: DryRun specifies whether the template should be applied
                  after validation or only validated.
                type: boolean
              ipamClaim:
                description: |-
                  IPAMClaim defines IP Address Management (IPAM) requirements for the cluster.
                  It can either reference an existing IPAM claim or specify an inline claim.
                properties:
                  ref:
                    description: ClusterIPAMClaimRef is the name of an existing ClusterIPAMClaim
                      resource to use.
                    type: string
                  spec:
                    description: |-
                      ClusterIPAMClaimSpec defines the inline IPAM claim specification if no reference is provided.
                      This allows for dynamic IP address allocation during cluster provisioning.
                    properties:
                      cluster:
                        description: Cluster is the reference to the [ClusterDeployment]
                          that this claim is for
                        type: string
                        x-kubernetes-validations:
                        - message: Cluster reference is immutable once set
                          rule: oldSelf == '' || self == oldSelf
                      clusterIPAMRef:
                        description: ClusterIPAMRef is the reference to the [ClusterIPAM]
                          resource that this claim is for
                        type: string
                        x-kubernetes-validations:
                        - message: ClusterIPAM reference is immutable once set
                          rule: oldSelf == '' || self == oldSelf
                      clusterNetwork:
                        description: ClusterNetwork defines the allocation for requisitioning
                          ip addresses for use by the k8s cluster itself
                        properties:
                          cidr:
                            description: CIDR notation of the allocated address space
                            type: string
                          gateway:
                            description: Gateway to be used for the address space
                            type: string
                          ipAddresses:
                            description: IPAddresses to be allocated
                            items:
                              type: string
                            type: array
                          prefix:
                            description: Prefix is the network prefix to use.
                            type: integer
                        type: object
                      externalNetwork:
                        description: ExternalNetwork defines the allocation for requisitioning
                          ip addresses for use by services such as load balancers
                        properties:
                          cidr:
                            description: CIDR notation of the allocated address space
                            type: string
                          gateway:
                            description: Gateway to be used for the address space
                            type: string
                          ipAddresses:
                            description: IPAddresses to be allocated
                            items:
                              type: string
                            type: array
                          prefix:
                            description: Prefix is the network prefix to use.
                            type: integer
                        type: object
                      nodeNetwork:
                        description: NodeNetwork defines the allocation requisitioning
                          ip addresses for cluster nodes
                        properties:
                          cidr:
                            description: CIDR notation of the allocated address space
                            type: string
                          gateway:
                            description: Gateway to be used for the address space
                            type: string
                          ipAddresses:
                            description: IPAddresses to be allocated
                            items:
                              type: string
                            type: array
                          prefix:
                            description: Prefix is the network prefix to use.
                            type: integer
                        type: object
                      provider:
                        description: Provider is the name of the provider that this
                          claim will be consumed by
                        enum:
                        - in-cluster
                        - ipam-infoblox
                        type: string
                    required:
                    - provider
                    type: object
                type: object
              propagateCredentials:
                default: true
                description: |-
                  PropagateCredentials indicates whether credentials should be propagated
                  for use by CCM (Cloud Controller Manager).
                type: boolean
              serviceSpec:
                description: ServiceSpec is spec related to deployment of services.
                properties:
                  continueOnError:
                    default: false
                    description: ContinueOnError specifies if the services deployment
                      should continue if an error occurs.
                    type: boolean
                  driftExclusions:
                    description: DriftExclusions specifies specific configurations
                      of resources to ignore for drift detection.
                    items:
                      properties:
                        paths:
                          description: Paths is a slice of JSON6902 paths to exclude
                            from configuration drift evaluation.
                          items:
                            type: string
                          type: array
                        target:
                          description: Target points to the resources that the paths
                            refers to.
                          properties:
                            annotationSelector:
                              description: |-
                                AnnotationSelector is a string that follows the label selection expression
                                https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#api
                                It matches with the resource annotations.
                              type: string
                            group:
                              description: |-
                                Group is the API group to select resources from.
                                Together with Version and Kind it is capable of unambiguously identifying and/or selecting resources.
                                https://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md
                              type: string
                            kind:
                              description: |-
                                Kind of the API Group to select resources from.
                                Together with Group and Version it is capable of unambiguously
                                identifying and/or selecting resources.
                                https://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md
                              type: string
                            labelSelector:
                              description: |-
                                LabelSelector is a string that follows the label selection expression
                                https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#api
                                It matches with the resource labels.
                              type: string
                            name:
                              description: Name to match resources with.
                              type: string
                            namespace:
                              description: Namespace to select resources from.
                              type: string
                            version:
                              description: |-
                                Version of the API Group to select resources from.
                                Together with Group and Kind it is capable of unambiguously identifying and/or selecting resources.
                                https://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md
                              type: string
                          type: object
                      required:
                      - paths
                      type: object
                    type: array
                  driftIgnore:
                    description: DriftIgnore specifies resources to ignore for drift
                      detection.
                    items:
                      properties:
                        annotationSelector:
                          description: |-
                            AnnotationSelector is a string that follows the label selection expression
                            https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#api
                            It matches with the resource annotations.
                          type: string
                        group:
                          description: |-
                            Group is the API group to select resources from.
                            Together with Version and Kind it is capable of unambiguously identifying and/or selecting resources.
                            https://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md
                          type: string
                        kind:
                          description: |-
                            Kind of the API Group to select resources from.
                            Together with Group and Version it is capable of unambiguously
                            identifying and/or selecting resources.
                            https://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md
                          type: string
                        labelSelector:
                          description: |-
                            LabelSelector is a string that follows the label selection expression
                            https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#api
                            It matches with the resource labels.
                          type: string
                        name:
                          description: Name to match resources with.
                          type: string
                        namespace:
                          description: Namespace to select resources from.
                          type: string
                        version:
                          description: |-
                            Version of the API Group to select resources from.
                            Together with Group and Kind it is capable of unambiguously identifying and/or selecting resources.
                            https://github.com/kubernetes/community/blob/master/contributors/design-proposals/api-machinery/api-group.md
                          type: string
                      type: object
                    type: array
                  priority:
                    default: 100
                    description: |-
                      Priority sets the priority for the services defined in this spec.
                      Higher value means higher priority and lower means lower.
                      In case of conflict with another object managing the service,
                      the one with higher priority will get to deploy its services.
                    format: int32
                    maximum: 2147483646
                    minimum: 1
                    type: integer
                  reload:
                    description: Reload instances via rolling upgrade when a ConfigMap/Secret
                      mounted as volume is modified.
                    type: boolean
                  services:
                    description: |-
                      Services is a list of services created via ServiceTemplates
                      that could be installed on the target cluster.
                    items:
                      description: Service represents a Service to be deployed.
                      properties:
                        disable:
                          description: Disable can be set to disable handling of this
                            service.
                          type: boolean
                        name:
                          description: Name is the chart release.
                          maxLength: 253
                          minLength: 1
                          type: string
                        namespace:
                          description: |-
                            Namespace is the namespace the release will be installed in.
                            It will default to Name if not provided.
                          type: string
                        template:
                          description: Template is a reference to a Template object
                            located in the same namespace.
                          maxLength: 253
                          minLength: 1
                          type: string
                        templateChain:
                          description: |-
                            TemplateChain defines the ServiceTemplateChain object that will be used to deploy the service
                            along with desired ServiceTemplate version.
                          type: string
                        values:
                          description: |-
                            Values is the helm values to be passed to the chart used by the template.
                            The string type is used in order to allow for templating.
                          type: string
                        valuesFrom:
                          description: ValuesFrom can reference a ConfigMap or Secret
                            containing helm values.
                          items:
                            properties:
                              kind:
                                description: |-
                                  Kind of the resource. Supported kinds are:
                                  - ConfigMap/Secret
                                enum:
                                - ConfigMap
                                - Secret
                                type: string
                              name:
                                description: |-
                                  Name of the referenced resource.
                                  Name can be expressed as a template and instantiate using any cluster field.
                                minLength: 1
                                type: string
                              namespace:
                                description: |-
                                  Namespace of the referenced resource.
                                  For ClusterProfile namespace can be left empty. In such a case, namespace will
                                  be implicit set to cluster's namespace.
                                  For Profile namespace must be left empty. The Profile namespace will be used.
                                  Namespace can be expressed as a template and instantiate using any cluster field.
                                type: string
                              optional:
                                default: false
                                description: |-
                                  Optional indicates that the referenced resource is not mandatory.
                                  If set to true and the resource is not found, the error will be ignored,
                                  and Sveltos will continue processing other ValueFroms.
                                type: boolean
                            required:
                            - kind
                            - name
                            type: object
                          type: array
                      required:
                      - name
                      - template
                      type: object
                    type: array
                  stopOnConflict:
                    default: false
                    description: |-
                      StopOnConflict specifies what to do in case of a conflict.
                      E.g. If another object is already managing a service.
                      By default the remaining services will be deployed even if conflict is detected.
                      If set to true, the deployment will stop after encountering the first conflict.
                    type: boolean
                  syncMode:
                    default: Continuous
                    description: SyncMode specifies how services are synced in the
                      target cluster.
                    enum:
                    - OneTime
                    - Continuous
                    - ContinuousWithDriftDetection
                    - DryRun
                    type: string
                  templateResourceRefs:
                    description: |-
                      TemplateResourceRefs is a list of resources to collect from the management cluster,
                      the values from which can be used in templates.
                    items:
                      properties:
                        identifier:
                          description: |-
                            Identifier is how the resource will be referred to in the
                            template
                          type: string
                        optional:
                          default: false
                          description: |-
                            Optional indicates that the referenced resource is not mandatory.
                            If set to true and the resource is not found, the error will be ignored,
                            and Sveltos will continue processing other TemplateResourceRefs.
                          type: boolean
                        resource:
                          description: |-
                            Resource references a Kubernetes instance in the management
                            cluster to fetch and use during template instantiation.
                            For ClusterProfile namespace can be left empty. In such a case, namespace will
                            be implicit set to cluster's namespace.
                            Name and namespace can be expressed as a template and instantiate using any cluster field.
                          properties:
                            apiVersion:
                              description: API version of the referent.
                              type: string
                            fieldPath:
                              description: |-
                                If referring to a piece of an object instead of an entire object, this string
                                should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                                For example, if the object reference is to a container within a pod, this would take on a value like:
                                "spec.containers{name}" (where "name" refers to the name of the container that triggered
                                the event) or if no container name is specified "spec.containers[2]" (container with
                                index 2 in this pod). This syntax is chosen only to have some well-defined way of
                                referencing a part of an object.
                              type: string
                            kind:
                              description: |-
                                Kind of the referent.
                                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                              type: string
                            name:
                              description: |-
                                Name of the referent.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            namespace:
                              description: |-
                                Namespace of the referent.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                              type: string
                            resourceVersion:
                              description: |-
                                Specific resourceVersion to which this reference is made, if any.
                                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                              type: string
                            uid:
                              description: |-
                                UID of the referent.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - identifier
                      - resource
                      type: object
                    type: array
                type: object
              template:
                description: Template is a reference to a Template object located
                  in the same namespace.
                maxLength: 253
                minLength: 1
                type: string
            required:
            - template
            type: object
          status:
            description: ClusterDeploymentStatus defines the observed state of ClusterDeployment
            properties:
              adoptedClusterStatus:
                description: AdoptedClusterStatus contains status information for
                  adopted cluster lifecycle management.
                properties:
                  backupStatus:
                    description: BackupStatus contains status information for backup
                      operations.
                    properties:
                      backupOperations:
                        description: BackupOperations contains information about recent
                          backup operations.
                        items:
                          description: BackupOperation defines a backup operation.
                          properties:
                            location:
                              description: Location indicates the location of the
                                backup.
                              type: string
                            message:
                              description: Message provides additional information
                                about the backup operation.
                              type: string
                            size:
                              description: Size indicates the size of the backup.
                              type: string
                            status:
                              description: Status indicates the status of this backup
                                operation.
                              type: string
                            timestamp:
                              description: Timestamp indicates when the backup operation
                                was performed.
                              format: date-time
                              type: string
                            type:
                              description: Type specifies the type of backup operation.
                              enum:
                              - full
                              - incremental
                              type: string
                          required:
                          - status
                          - timestamp
                          - type
                          type: object
                        type: array
                      lastBackupTime:
                        description: LastBackupTime indicates when the last backup
                          was performed.
                        format: date-time
                        type: string
                      message:
                        description: Message provides additional information about
                          the backup status.
                        type: string
                      phase:
                        description: Phase indicates the current phase of backup.
                        enum:
                        - NotStarted
                        - InProgress
                        - Completed
                        - Failed
                        type: string
                    required:
                    - phase
                    type: object
                  capiStatus:
                    description: CAPIStatus contains status information for CAPI integration.
                    properties:
                      generatedResources:
                        description: GeneratedResources contains information about
                          generated CAPI resources.
                        items:
                          description: GeneratedResource defines a generated CAPI
                            resource.
                          properties:
                            apiVersion:
                              description: APIVersion specifies the API version of
                                the generated resource.
                              type: string
                            kind:
                              description: Kind specifies the kind of the generated
                                resource.
                              type: string
                            name:
                              description: Name specifies the name of the generated
                                resource.
                              type: string
                            namespace:
                              description: Namespace specifies the namespace of the
                                generated resource.
                              type: string
                            status:
                              description: Status indicates the status of this resource.
                              type: string
                          required:
                          - apiVersion
                          - kind
                          - name
                          type: object
                        type: array
                      lastSyncTime:
                        description: LastSyncTime indicates when the last CAPI sync
                          was performed.
                        format: date-time
                        type: string
                      message:
                        description: Message provides additional information about
                          the CAPI integration status.
                        type: string
                      phase:
                        description: Phase indicates the current phase of CAPI integration.
                        enum:
                        - NotStarted
                        - InProgress
                        - Completed
                        - Failed
                        type: string
                    required:
                    - phase
                    type: object
                  discoveryStatus:
                    description: DiscoveryStatus contains status information for infrastructure
                      discovery.
                    properties:
                      discoveredResources:
                        description: DiscoveredResources contains information about
                          discovered infrastructure resources.
                        items:
                          description: DiscoveredResource defines a discovered infrastructure
                            resource.
                          properties:
                            name:
                              description: Name specifies the name of the discovered
                                resource.
                              type: string
                            provider:
                              description: Provider specifies the cloud provider for
                                this resource.
                              type: string
                            region:
                              description: Region specifies the region where this
                                resource is located.
                              type: string
                            status:
                              description: Status indicates the status of this resource.
                              type: string
                            type:
                              description: Type specifies the type of discovered resource.
                              type: string
                          required:
                          - name
                          - type
                          type: object
                        type: array
                      lastDiscoveryTime:
                        description: LastDiscoveryTime indicates when the last discovery
                          was performed.
                        format: date-time
                        type: string
                      message:
                        description: Message provides additional information about
                          the discovery status.
                        type: string
                      phase:
                        description: Phase indicates the current phase of discovery.
                        enum:
                        - NotStarted
                        - InProgress
                        - Completed
                        - Failed
                        type: string
                    required:
                    - phase
                    type: object
                  monitoringStatus:
                    description: MonitoringStatus contains status information for
                      monitoring.
                    properties:
                      healthStatus:
                        description: HealthStatus indicates the overall health status
                          of the cluster.
                        type: string
                      lastHealthCheckTime:
                        description: LastHealthCheckTime indicates when the last health
                          check was performed.
                        format: date-time
                        type: string
                      message:
                        description: Message provides additional information about
                          the monitoring status.
                        type: string
                      metrics:
                        description: Metrics contains collected metrics information.
                        items:
                          description: MetricInfo defines metric information.
                          properties:
                            name:
                              description: Name specifies the name of the metric.
                              type: string
                            timestamp:
                              description: Timestamp indicates when this metric was
                                collected.
                              format: date-time
                              type: string
                            unit:
                              description: Unit specifies the unit of the metric.
                              type: string
                            value:
                              description: Value specifies the current value of the
                                metric.
                              type: string
                          required:
                          - name
                          - timestamp
                          type: object
                        type: array
                      phase:
                        description: Phase indicates the current phase of monitoring.
                        enum:
                        - NotStarted
                        - InProgress
                        - Completed
                        - Failed
                        type: string
                    required:
                    - phase
                    type: object
                  scalingStatus:
                    description: ScalingStatus contains status information for scaling
                      operations.
                    properties:
                      currentNodes:
                        description: CurrentNodes indicates the current number of
                          nodes.
                        format: int32
                        type: integer
                      desiredNodes:
                        description: DesiredNodes indicates the desired number of
                          nodes.
                        format: int32
                        type: integer
                      lastScalingTime:
                        description: LastScalingTime indicates when the last scaling
                          operation was performed.
                        format: date-time
                        type: string
                      message:
                        description: Message provides additional information about
                          the scaling status.
                        type: string
                      phase:
                        description: Phase indicates the current phase of scaling.
                        enum:
                        - NotStarted
                        - InProgress
                        - Completed
                        - Failed
                        type: string
                      scalingOperations:
                        description: ScalingOperations contains information about
                          recent scaling operations.
                        items:
                          description: ScalingOperation defines a scaling operation.
                          properties:
                            fromNodes:
                              description: FromNodes indicates the number of nodes
                                before scaling.
                              format: int32
                              type: integer
                            message:
                              description: Message provides additional information
                                about the scaling operation.
                              type: string
                            status:
                              description: Status indicates the status of this scaling
                                operation.
                              type: string
                            timestamp:
                              description: Timestamp indicates when the scaling operation
                                was performed.
                              format: date-time
                              type: string
                            toNodes:
                              description: ToNodes indicates the number of nodes after
                                scaling.
                              format: int32
                              type: integer
                            type:
                              description: Type specifies the type of scaling operation.
                              enum:
                              - scale-up
                              - scale-down
                              type: string
                          required:
                          - fromNodes
                          - status
                          - timestamp
                          - toNodes
                          - type
                          type: object
                        type: array
                    required:
                    - phase
                    type: object
                  upgradeStatus:
                    description: UpgradeStatus contains status information for upgrade
                      operations.
                    properties:
                      currentVersion:
                        description: CurrentVersion indicates the current Kubernetes
                          version.
                        type: string
                      lastUpgradeTime:
                        description: LastUpgradeTime indicates when the last upgrade
                          was performed.
                        format: date-time
                        type: string
                      message:
                        description: Message provides additional information about
                          the upgrade status.
                        type: string
                      phase:
                        description: Phase indicates the current phase of upgrade.
                        enum:
                        - NotStarted
                        - InProgress
                        - Completed
                        - Failed
                        type: string
                      targetVersion:
                        description: TargetVersion indicates the target Kubernetes
                          version for upgrade.
                        type: string
                      upgradeOperations:
                        description: UpgradeOperations contains information about
                          recent upgrade operations.
                        items:
                          description: UpgradeOperation defines an upgrade operation.
                          properties:
                            fromVersion:
                              description: FromVersion indicates the version before
                                upgrade.
                              type: string
                            message:
                              description: Message provides additional information
                                about the upgrade operation.
                              type: string
                            status:
                              description: Status indicates the status of this upgrade
                                operation.
                              type: string
                            timestamp:
                              description: Timestamp indicates when the upgrade operation
                                was performed.
                              format: date-time
                              type: string
                            toVersion:
                              description: ToVersion indicates the version after upgrade.
                              type: string
                            type:
                              description: Type specifies the type of upgrade operation.
                              enum:
                              - control-plane
                              - node-pool
                              - infrastructure
                              type: string
                          required:
                          - fromVersion
                          - status
                          - timestamp
                          - toVersion
                          - type
                          type: object
                        type: array
                    required:
                    - phase
                    type: object
                type: object
              availableUpgrades:
                description: |-
                  AvailableUpgrades is the list of ClusterTemplate names to which
                  this cluster can be upgraded. It can be an empty array, which means no upgrades are
                  available.
                items:
                  type: string
                type: array
              conditions:
                description: Conditions contains details for the current state of
                  the ClusterDeployment.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              k8sVersion:
                description: |-
                  Currently compatible exact Kubernetes version of the cluster. Being set only if
                  provided by the corresponding ClusterTemplate.
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation.
                format: int64
                type: integer
              services:
                description: Services contains details for the state of services.
                items:
                  description: ServiceStatus contains details for the state of services.
                  properties:
                    clusterName:
                      description: ClusterName is the name of the associated cluster.
                      type: string
                    clusterNamespace:
                      description: ClusterNamespace is the namespace of the associated
                        cluster.
                      type: string
                    conditions:
                      description: Conditions contains details for the current state
                        of managed services.
                      items:
                        description: Condition contains details for one aspect of
                          the current state of this API Resource.
                        properties:
                          lastTransitionTime:
                            description: |-
                              lastTransitionTime is the last time the condition transitioned from one status to another.
                              This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                            format: date-time
                            type: string
                          message:
                            description: |-
                              message is a human readable message indicating details about the transition.
                              This may be an empty string.
                            maxLength: 32768
                            type: string
                          observedGeneration:
                            description: |-
                              observedGeneration represents the .metadata.generation that the condition was set based upon.
                              For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                              with respect to the current state of the instance.
                            format: int64
                            minimum: 0
                            type: integer
                          reason:
                            description: |-
                              reason contains a programmatic identifier indicating the reason for the condition's last transition.
                              Producers of specific condition types may define expected values and meanings for this field,
                              and whether the values are considered a guaranteed API.
                              The value should be a CamelCase string.
                              This field may not be empty.
                            maxLength: 1024
                            minLength: 1
                            pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                            type: string
                          status:
                            description: status of the condition, one of True, False,
                              Unknown.
                            enum:
                            - "True"
                            - "False"
                            - Unknown
                            type: string
                          type:
                            description: type of condition in CamelCase or in foo.example.com/CamelCase.
                            maxLength: 316
                            pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                            type: string
                        required:
                        - lastTransitionTime
                        - message
                        - reason
                        - status
                        - type
                        type: object
                      type: array
                      x-kubernetes-list-map-keys:
                      - type
                      x-kubernetes-list-type: map
                  required:
                  - clusterName
                  type: object
                type: array
              servicesUpgradePaths:
                description: ServicesUpgradePaths contains details for the state of
                  services upgrade paths.
                items:
                  description: ServiceUpgradePaths contains details for the state
                    of service upgrade paths.
                  properties:
                    availableUpgrades:
                      description: AvailableUpgrades contains details for the state
                        of available upgrades.
                      items:
                        description: UpgradePath contains details for the state of
                          service upgrade paths.
                        properties:
                          upgradePaths:
                            description: Versions contains the list of versions that
                              service can be upgraded to.
                            items:
                              type: string
                            type: array
                        type: object
                      type: array
                    name:
                      description: Name is the name of the service.
                      type: string
                    namespace:
                      description: Namespace is the namespace of the service.
                      type: string
                    template:
                      description: Template is the name of the current service template.
                      type: string
                  required:
                  - name
                  - namespace
                  - template
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
